---
phase: 01-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - 01-05
files_modified: []
autonomous: false
requirements:
  - FOUN-01
  - FOUN-04
  - FOUN-05
  - FOUN-06
  - FOUN-07
  - FOUN-08
  - FOUN-10
  - FOUN-11
  - FOUN-12

must_haves:
  truths:
    - "Plugin loads in Obsidian without console errors"
    - "Settings tab shows grouped API key fields, debounce slider, and debug toggle"
    - "Status bar reflects canvas state changes (idle/thinking/hidden)"
    - "Canvas events trigger debounce and status bar transitions"
    - "Per-canvas toggle works via context menu"
---

<objective>
Manual verification of the complete Phase 1 foundation in a live Obsidian environment.

Purpose: Canvas plugin functionality cannot be fully tested outside Obsidian's Electron runtime. This checkpoint verifies all UI surfaces, event detection, debounce behavior, and state transitions work correctly in the actual runtime environment.

Output: Verified working plugin, ready for Phase 2 development.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build and plugin installation prep</name>
  <files>main.js, manifest.json, styles.css</files>
  <action>
1. Run `npm run build` to produce the production build.
2. Verify the output files exist: `main.js`, `manifest.json`, `styles.css`.
3. Report the file sizes to confirm they are reasonable (main.js should be under 100KB for Phase 1).
4. Run `npx tsc --noEmit` to confirm zero type errors.
5. Run `npx jest --bail` to confirm all tests pass.
6. Provide instructions for installing the plugin in Obsidian:
   - Create a folder in the user's Obsidian vault: `.obsidian/plugins/canvas-ai/`
   - Copy `main.js`, `manifest.json`, and `styles.css` into that folder
   - Restart Obsidian or reload plugins
   - Enable "Canvas AI" in Settings -> Community plugins
  </action>
  <verify>
    <automated>cd /Users/hoydbreton/Developer/obsidian && npm run build && test -f main.js && test -f manifest.json && test -f styles.css && npx tsc --noEmit && npx jest --bail && echo "PASS: Production build ready"</automated>
  </verify>
  <done>Production build artifacts ready for Obsidian installation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 plugin in live Obsidian</name>
  <files>main.js, manifest.json, styles.css</files>
  <action>
Install the plugin in Obsidian by copying main.js, manifest.json, and styles.css to .obsidian/plugins/canvas-ai/, then verify every Phase 1 requirement in the live environment.

What was built: Complete Phase 1 Foundation plugin including settings tab with API keys + debounce slider + debug toggle, status bar indicator with state management and popover, per-canvas enable/disable via context menu and command palette, canvas event detection (create/edit/move/delete) via monkey-patching, debounce timer with AbortController cancellation.

Verification steps:

1. Plugin Load (FOUN-01): Enable "Canvas AI" in Settings -> Community plugins. Open developer console (Cmd+Option+I). Confirm no errors on load. Status bar should show "AI: no API key" in red. A notice should appear once: "Canvas AI: Set up your API keys in Settings -> Canvas AI".

2. Settings Tab (FOUN-10): Go to Settings -> Canvas AI. Verify "API Keys" heading with Claude and Runware text fields. Verify "Behavior" heading with debounce slider and debug toggle. Type "sk-ant-test123" in Claude API key field -> green "Format OK" text. Type "invalid" -> red "Invalid key format" text. Clear field -> original description returns. Drag debounce slider -> tooltip shows 1-10. Toggle debug mode.

3. Status Bar (FOUN-12): Enter fake API key "sk-ant-test". Open a .canvas file -> "AI: idle" in muted color. Click status bar -> popover: "State: idle" and "Last trigger: never". Switch to non-canvas file -> hidden. Switch back -> reappears.

4. Canvas Events (FOUN-04-07, D-14): Enable debug mode. Open console. Create text node -> "canvas-ai:node-created" logged. Edit node -> "canvas-ai:canvas-changed" logged. Move node -> "canvas-ai:node-moved" logged. Delete node -> "canvas-ai:node-removed" logged.

5. Debounce (FOUN-08): With API key set, perform canvas action. Wait ~3s -> "AI: thinking" briefly then "AI: idle". Rapid actions -> only one thinking cycle after stopping.

6. Per-Canvas Toggle (FOUN-11): Right-click node -> "Disable Canvas AI" in menu. Click it -> status bar hides. Right-click -> "Enable Canvas AI". Click -> status bar returns. Command palette "Toggle Canvas AI" available.

Resume signal: Type "approved" or describe issues found during verification.
  </action>
  <verify>
    <automated>MISSING -- manual verification in Obsidian runtime required</automated>
  </verify>
  <done>All Phase 1 requirements verified in live Obsidian. Plugin is ready for Phase 2: Spatial Intelligence.</done>
</task>

</tasks>

<verification>
- Plugin loads without errors in Obsidian
- All 4 event types detected and logged in debug mode
- Status bar state transitions correct across all scenarios
- Settings tab layout matches UI-SPEC
- Per-canvas toggle works via context menu and command palette
</verification>

<success_criteria>
- Plugin installs, loads, and appears in community plugins (FOUN-01)
- All settings fields functional: API keys, debounce slider, debug toggle (FOUN-10)
- Status bar shows correct states and responds to all transitions (FOUN-12)
- Canvas events detected: create, edit, move, delete (FOUN-04-07)
- Debounce fires after idle, resets on new activity (FOUN-08)
- Per-canvas enable/disable via context menu and command palette (FOUN-11)
- Phase 1 complete -- ready for Phase 2: Spatial Intelligence
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
